<%= form_for @comment, url: weekend_event_comments_path do |f| %>
	<%= render 'shared/error_messages', :object=>f.object %>
	
	<div>
		<%= f.label :content %>
		<%= f.text_field :content %>
	</div>
	
	<div>
		<%= f.submit 'save comment' %>
	</div>
	
<% end %> 