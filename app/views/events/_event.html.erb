<div>
	<%= event.title %>: 
	<%= link_to 'Edit', edit_weekend_event_path(id:event.id, weekend_id:params[:id]) %> 
	| <%= link_to 'Show', weekend_event_path(id:event.id, weekend_id:params[:id]) %> 
</div>
